#include <iostream>
using namespace std;


int main() {
    int n;
    cin >> n;
    vector<string> vec_name;
    vector<int> vec_population;
    for (int i = 0; i < n; i++) {
        string name;
        int population;
        cin >> name >> population;
        vec_name.push_back(name);

    }
}
